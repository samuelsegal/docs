export const bubbleSortData = Array.apply(null, { length: 10000 }).map(Function.call, Math.random);
export const mergeSortData = Array.apply(null, { length: 10000 }).map(Function.call, Math.random);
export const insertionSortData = Array.apply(null, { length: 10000 }).map(Function.call, Math.random);
export const selectionSortData = Array.apply(null, { length: 10000 }).map(Function.call, Math.random);
export const quickSortData = Array.apply(null, { length: 1000 }).map(Function.call, Math.random);
